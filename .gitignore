/target
# dist 不入库:构建产物 / 浏览器 / 预编译二进制 / zip 等大文件,均属运行产物,不随源码发布。
/dist/
# 内部文档不入库:开发日志(进度.md)与历史需求快照(需求.md,已被实际进展覆盖);
# 公开文档(设计 / 并发池 / API映射 / 长监听与滑动 / docs/README.md)随仓库发布。
/docs/进度.md
/docs/需求.md
**/*.rs.bk
.DS_Store
*.log
# 示例运行产物(可能含本机路径/测试数据,不入库不发布)
/drission_*_result.json
# 验证码 / 截图类示例产物(根目录兜底;示例本应写到 target/ 下)
/*.png
/*.jpg
/yidun_*.png
# 易盾点选样本采集产物(自训模型用的图片/CSV,体积大且属运行产物,不入库)
/yidun_samples/
# 自训模型脚手架(dddd_trainer 克隆 + venv + 标注工具/数据,纯开发产物,不入库)
/yidun-train/
# 录像 / 采集导出等运行产物(示例本应写 target/;根目录兜底)
/*.mp4
# 吐环境运行产物(含真实 cookie/webid/指纹 等,不入库)
/dump-env/
/dump-env-fp/
/douyin-env/
# 含真实 cookie/签名的测试脚本(不入库)
/scripts/douyin_detail_test.py
# 逆向子项目(含会员登录态 / 抓包产物 / 自训样本,严禁上传公开仓库)
/luffycity/
/beian-probe/
/beian-probe.zip
/yidun-env/
# 抓包 / 下载产物(luffycity 会员下载的视频、polyv_secure、requests.jsonl、解密 key 等)
/captures/
